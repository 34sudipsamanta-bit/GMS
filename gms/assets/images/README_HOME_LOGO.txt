Place your new home/logo image at this path to activate it across the site:

File path: gms/assets/images/home-logo.png

Recommended steps:
1. Save the attached PNG you provided as `home-logo.png`.
2. Put the file under: `gms/assets/images/home-logo.png`.
3. Clear the browser cache and reload pages.

Notes:
- The site now hides inline base64 data-URI logos and displays the static `home-logo.png` for the navbar and auth pages where supported.
- If you want me to extract the attached PNG into the repo for you, upload the image as base64 or allow me to write binary into the workspace.